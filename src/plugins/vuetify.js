import { createVuetify } from "vuetify";
import * as components from "vuetify/components";
import * as directives from "vuetify/directives";
import "@mdi/font/css/materialdesignicons.css";
import "vuetify/styles";

export const vuetify = createVuetify({
  components,
  directives,
  theme: {
    defaultTheme: 'light',
    themes: {
      light: {
        dark: false,
        colors: {
          primary: '#1867C0',   // The classic Blue
          secondary: '#5CBBF6', // Lighter Blue
          accent: '#82B1FF',
          error: '#FF5252',
          info: '#2196F3',
          success: '#4CAF50',
          warning: '#FB8C00',
          // THIS is what fixes the invisible text:
          'on-primary': '#FFFFFF', 
          'on-secondary': '#FFFFFF',
        },
      },
    },
  },
});